# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# ========== 🚀 编译速度优化 ==========

# 🔥 JVM 内存配置 - 增加到 8GB，切换 G1GC 以提升大工程与 R8 混淆极速编译性能
org.gradle.jvmargs=-Xmx8192m -XX:+UseG1GC -Dfile.encoding=UTF-8

# 🔥 启用并行编译 - 多模块时效果显著
org.gradle.parallel=true

# 🔥 启用配置缓存 - 缓存配置阶段，增量编译更快
org.gradle.configuration-cache=true

# 🔥 多模块工程并行存取配置缓存，缩短正式版构建的配置阶段
# 如遇 Gradle 配置阶段的 ConcurrentModificationException，可单独通过
# -Dorg.gradle.configuration-cache.parallel=false 回退。
org.gradle.configuration-cache.parallel=true

# 🔥 启用构建缓存 - 缓存编译输出，跨构建复用
org.gradle.caching=true

# 🔥 文件系统监听 - 提升重复构建时的变更检测速度
org.gradle.vfs.watch=true

# ⚠️ 在配置缓存已经开启的前提下，按需配置收益很小，且更容易放大
# Kotlin/KSP 首次重建时的边缘问题，这里关闭以换取更稳定的本地编译体验。
org.gradle.configureondemand=false

# ========== Kotlin 优化 ==========

# 🔥 Kotlin 增量编译
kotlin.incremental=true

# 🔥 Kotlin 守护进程保持更长时间，避免频繁重启
kotlin.daemon.jvmargs=-Xmx4096m

# 🔥 KSP 增量处理 - Room 等注解处理器在小改动时可少做很多重复工作
ksp.incremental=true
ksp.incremental.intermodule=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# ========== Android 优化 ==========

# AndroidX package structure
android.useAndroidX=true


# compileSdk 37 与当前 AGP 组合时若仍有预览 SDK 提示，按最小范围抑制。
android.suppressUnsupportedCompileSdk=37,37.0

# 🔥 非传递 R 类 - 减少 R 类大小
android.nonTransitiveRClass=true

# ========== Firebase 优化 ==========

# 🔥 禁用 Crashlytics mapping 文件自动上传（避免网络问题导致编译失败）
firebaseCrashlytics.mappingFileUploadEnabled=false

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true
